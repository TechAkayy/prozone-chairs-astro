---
import BaseHead from '@/components/BaseHead.astro'

// import NavBar from '@/components/NavBar.vue'
// import TheFooter from '@/components/TheFooter.vue'
import site from '@/site'
import { heroImageUrl } from '@/utils/hero'

const { name: SITE_TITLE, description: SITE_DESCRIPTION } = site

export interface Props {
  title: string
}

const { title } = Astro.props
const currentPath = Astro.url.pathname
---

<script src="../plugins/youtube.client.ts"></script>

<html lang="en">
  <head>
    <BaseHead
      title={title || SITE_TITLE}
      description={SITE_DESCRIPTION}
      image={heroImageUrl}
    />
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/pikaday/css/pikaday.css"
    />
    <script is:inline src="https://cdn.jsdelivr.net/npm/pikaday/pikaday.js"></script>
  </head>
  <body>
    <div>
      <!-- <div class="container mx-auto"> -->
      <div
        class="min-h-screen flex flex-col bg-white dark:bg-neutral-950 text-neutral-900 dark:text-neutral-50"
      >

        <!-- <NavBar
          class={currentPath === '/' ? 'absolute bg-transparent z-10' : ''}
          client:load
        /> -->
        <main class="shadow">
          <slot />
        </main>
        <!-- <TheFooter /> -->
      </div>
      <!-- </div> -->
    </div>
    <style is:global>
      /* :root {
    --accent: 124, 58, 237;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      #da62c4 30%,
      white 60%
    );
  }
  html {
    font-family: system-ui, sans-serif;
    background-color: #f6f6f6;
  }
  code {
    font-family: Menlo, Monaco, Lucida Console, Liberation Mono,
      DejaVu Sans Mono, Bitstream Vera Sans Mono, Courier New, monospace;
  } */
    </style>
  </body>
</html>
